# BGL Architecture Documentation

## 📁 هيكل المشروع

```
BGL/
├── app/                      # كود التطبيق الرئيسي (PHP)
│   ├── Controllers/          # المتحكمات - تستقبل الطلبات وترجع JSON
│   ├── Models/               # الكائنات (Data Objects)
│   ├── Repositories/         # طبقة الوصول لقاعدة البيانات
│   ├── Services/             # منطق الأعمال
│   ├── Support/              # أدوات مساعدة (Normalizer, Logger, etc.)
│   └── Views/                # قوالب الواجهات
│       ├── pages/            # صفحات كاملة (decision-page, stats, reports, etc.)
│       ├── letters/          # قوالب الطباعة (print-letter)
│       └── partials/         # قطع مشتركة (headers, footers)
├── storage/
│   ├── database/app.sqlite   # قاعدة البيانات
│   ├── backups/              # النسخ الاحتياطية
│   └── uploads/              # ملفات Excel المرفوعة
└── www/                      # الملفات العامة فقط
    ├── index.php             # نقطة الدخول + Router
    ├── settings.php          # صفحة الإعدادات
    ├── includes/             # ملفات مساعدة
    │   ├── router.php        # API router
    │   └── views-router.php  # Views router
    ├── api/                  # API endpoints مستقلة
    └── assets/               # ملفات ثابتة
        ├── css/              # أنماط CSS
        └── js/               # ملفات JavaScript
```

---

## 🔄 تدفق البيانات

```
┌─────────────────────────────────────────────────────────────────┐
│                          المستخدم                               │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     www/index.php (Router)                      │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  GET /api/records        → DecisionController::index()          │
│  POST /api/decision/{id} → DecisionController::saveDecision()   │
│  POST /api/upload        → ImportController::upload()           │
│  GET /api/stats          → StatsController::index()             │
│  GET /api/dictionary/*   → DictionaryController::*()            │
│  GET /api/settings       → SettingsController::all()            │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Controllers                               │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  • التحقق من المدخلات                                           │
│  • استدعاء Services و Repositories                              │
│  • إرجاع JSON response                                          │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Services                                 │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ImportService     → استيراد ملفات Excel                       │
│  MatchingService   → مطابقة الموردين والبنوك                    │
│  CandidateService  → إيجاد المرشحين للمطابقة                    │
│  AutoAcceptService → القبول التلقائي للتطابقات العالية          │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Repositories                               │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ImportedRecordRepository  → جدول imported_records              │
│  SupplierRepository        → جدول suppliers (القاموس)           │
│  BankRepository            → جدول banks (القاموس)               │
│  *LearningRepository       → جداول التعلم                       │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SQLite Database                               │
│  storage/database/app.sqlite                                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📦 الـ Controllers

| Controller | الملف | الوظيفة |
|------------|-------|---------|
| `DecisionController` | 22KB | إدارة قرارات المطابقة للسجلات |
| `ImportController` | 2.5KB | استيراد ملفات Excel |
| `DictionaryController` | 12KB | إدارة قواميس الموردين والبنوك |
| `SettingsController` | 5.6KB | إعدادات التطبيق والنسخ الاحتياطي |
| `StatsController` | 0.8KB | إحصائيات لوحة التحكم |
| `ReportController` | 2.8KB | تصدير التقارير |

---

## ⚙️ الـ Services

| Service | الوظيفة |
|---------|---------|
| `ImportService` | قراءة ملفات Excel وإنشاء سجلات |
| `MatchingService` | مطابقة الأسماء الخام مع القواميس |
| `CandidateService` | إيجاد قائمة المرشحين للمطابقة |
| `AutoAcceptService` | قبول تلقائي عند تطابق عالي |
| `ConflictDetector` | اكتشاف التعارضات في المطابقات |

---

## 🗄️ قاعدة البيانات (SQLite)

### الجداول الرئيسية

```sql
-- السجلات المستوردة من Excel
imported_records (
    id, session_id, 
    raw_supplier_name, raw_bank_name,
    supplier_id, bank_id,           -- FK to dictionaries
    supplier_display_name,          -- الاسم المعروض (مجمّد)
    match_status,                   -- pending/needs_review/ready
    amount, guarantee_number, ...
)

-- قاموس الموردين
suppliers (id, official_name, normalized_name, supplier_normalized_key, is_confirmed)

-- قاموس البنوك
banks (id, official_name, normalized_key, short_code, department, address_line_1, ...)

-- الأسماء البديلة للموردين (يدوية)
supplier_alternative_names (id, supplier_id, raw_name, normalized_key)
```

### جداول التعلم (Phase 4 - 2025-12-17)

```sql
-- ⭐ كاش الاقتراحات (Cache-First Architecture)
supplier_suggestions (
    id,
    normalized_input,      -- الاسم المطبّع من Excel
    supplier_id,           -- المورد المقترح
    display_name,          -- الاسم للعرض
    source,                -- dictionary/learning/user_history
    fuzzy_score,           -- 0.0 - 1.0
    source_weight,         -- 40-100
    usage_count,           -- عدد مرات الاختيار
    block_count,           -- عدد مرات الحظر (للحظر التدريجي)
    total_score,           -- النقاط الإجمالية
    star_rating            -- 1-3 نجوم
)


-- سجل كل قرارات المستخدم
user_decisions (
    id,
    record_id, session_id,
    raw_name, normalized_name,
    chosen_supplier_id, chosen_display_name,
    decision_source,       -- user_click/propagation/auto_select/import
    decided_at
)

-- ⭐ سجل الأحداث الزمني (Timeline Events) - New in v2.1.0
guarantee_timeline_events (
    id,
    guarantee_number, record_id, session_id,
    event_type,            -- supplier_change, bank_change, extension, etc.
    field_name,            -- supplier, bank, amount, expiry_date
    old_value, new_value,
    old_id, new_id,
    supplier_display_name, bank_display,
    change_type,           -- entity_change, correction, update, action
    created_at
)

-- تعلم البنوك
bank_aliases_learning (
    id,
    normalized_input, input_name,
    status,                -- alias/blocked
    bank_id,
    usage_count, last_used_at
)
```

### حالات السجلات (match_status)

| الحالة | المعنى | متى تتغير |
|--------|--------|-----------|
| `pending` | جديد من الاستيراد | عند الإنشاء |
| `needs_review` | يحتاج مراجعة | تطابق منخفض (<90%) |
| `ready` | جاهز للطباعة | تم اختيار المورد والبنك |

> **ملاحظة**: `approved` يُحوّل تلقائياً إلى `ready` للتبسيط.

---

## 🖥️ JavaScript (الوضع الحالي)

### ملفات صفحة القرار (`www/assets/js/decision/`)

| الملف | الحجم | الوظيفة |
|-------|-------|---------|
| `decision.js` | 81KB | المنطق الرئيسي للصفحة |
| `state.js` | 5KB | إدارة الحالة |
| `api.js` | 5KB | طلبات AJAX |
| `index.js` | 3KB | نقطة الدخول |
| `ui/` | ~10KB | مكونات الواجهة |
| `logic/` | ~5KB | منطق إضافي |

### ملفات أخرى

| الملف | الحجم | الوظيفة |
|-------|-------|---------|
| `settings.js` | 18KB | صفحة الإعدادات |
| `stats.js` | 3KB | صفحة الإحصائيات |

---

## 🔑 المفاهيم الأساسية

### 1. Session (الجلسة)
كل عملية استيراد Excel تُنشئ جلسة جديدة. السجلات تُفلتر حسب الجلسة.

### 2. Match Status (حالة المطابقة)
- `pending`: بانتظار المطابقة
- `needs_review`: يحتاج مراجعة يدوية
- `ready`: جاهز للاعتماد (مورد + بنك)
- `approved`: تم اعتماده نهائياً

### 3. Normalization (التطبيع)
تحويل الأسماء لصيغة موحدة للمقارنة:
- إزالة المسافات الزائدة
- توحيد الأحرف العربية
- إزالة الرموز الخاصة

### 4. Learning (التعلم)
حفظ قرارات المستخدم لاستخدامها في المطابقات المستقبلية:
- `alias`: ربط اسم خام بمورد/بنك
- `blocked`: حظر اقتراح معين

---

## 🎯 خطة التحول لـ PHP-First

راجع: [implementation_plan.md](./implementation_plan.md)
