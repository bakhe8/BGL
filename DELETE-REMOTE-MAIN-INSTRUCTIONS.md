# تعليمات حذف الفرع `main` البعيد من GitHub

## ⚠️ الوضع الحالي

تم حذف الفرع `main` **محلياً** بنجاح، لكنه لا يزال موجوداً على GitHub كفرع افتراضي.

**لماذا لم يُحذف تلقائياً؟**  
GitHub يمنع حذف الفرع الافتراضي للمستودع كإجراء حماية.

---

## الخطوات المطلوبة

### الخيار 1: تغيير الفرع الافتراضي عبر واجهة GitHub (موصى به)

1. **افتح المستودع على GitHub**:
   - اذهب إلى: https://github.com/bakhe8/BGL

2. **ادخل إلى الإعدادات**:
   - اضغط على `Settings` من القائمة العلوية

3. **غيّر الفرع الافتراضي**:
   - في القسم الجانبي، اذهب إلى `Branches`
   - ستجد "Default branch" يشير إلى `main`
   - اضغط على السهم أو زر التبديل بجانب `main`
   - اختر `php-first-restructure` من القائمة المنسدلة
   - اضغط `Update` ثم أكد التغيير

4. **احذف الفرع القديم**:
   الآن بعد تغيير الفرع الافتراضي، يمكنك:
   
   **عبر واجهة GitHub**:
   - اذهب إلى: https://github.com/bakhe8/BGL/branches
   - ابحث عن الفرع `main` في قائمة الفروع
   - اضغط على أيقونة سلة المهملات 🗑️ بجانب `main`
   - أكد الحذف

   **أو عبر سطر الأوامر**:
   ```bash
   # من مجلد المشروع
   git push origin --delete main
   ```

---

### الخيار 2: الاحتفاظ بالفرع `main` كأرشيف

إذا كنت تريد الاحتفاظ بـ `main` كأرشيف (للمرجع فقط):

1. غيّر الفرع الافتراضي إلى `php-first-restructure` (الخطوات أعلاه)
2. **لا تحذف** الفرع `main`
3. أضف وصف للفرع `main` يوضح أنه أرشيف:
   - اذهب إلى: https://github.com/bakhe8/BGL/branches
   - اضغط على أيقونة القلم بجانب `main`
   - أضف وصف: "Archived - Old main branch before restructure"

---

### الخيار 3: إعادة تسمية الفرع (خيار متقدم)

إذا كنت تريد أن يكون اسم الفرع الرئيسي `main` بدلاً من `php-first-restructure`:

```bash
# محلياً: إعادة تسمية الفرع الحالي
git branch -m php-first-restructure main

# حذف الفرع القديم من البعيد
git push origin --delete main

# دفع الفرع الجديد
git push origin main

# تحديث المسار البعيد
git push origin -u main
```

ثم على GitHub:
- غيّر الفرع الافتراضي إلى `main`
- احذف `php-first-restructure` القديم

---

## التحقق من النجاح

بعد إكمال الخطوات:

1. **تأكد من الفرع الافتراضي**:
   - اذهب إلى: https://github.com/bakhe8/BGL
   - يجب أن ترى الفرع الافتراضي الجديد في القائمة

2. **تحقق من الفروع**:
   - اذهب إلى: https://github.com/bakhe8/BGL/branches
   - يجب أن ترى الفروع المتبقية فقط

3. **محلياً: تحديث المراجع**:
   ```bash
   git fetch --prune
   git branch -a
   ```
   يجب ألا ترى `remotes/origin/main` إذا حذفته

---

## ⚠️ تحذيرات

- **قبل الحذف**: تأكد 100% أن جميع التغييرات المهمة من `main` موجودة في `php-first-restructure`
- **النسخة الاحتياطية**: لديك نسخة احتياطية كاملة في:
  ```
  C:\Users\Bakheet\Documents\Projects\BGL-BACKUP-2025-12-17
  ```
- **لا يمكن التراجع**: حذف الفرع من GitHub نهائي (إلا إذا استعدته خلال 90 يوم من "Recently deleted")

---

## في حالة الخطأ

إذا حذفت الفرع بالخطأ:

1. اذهب إلى: https://github.com/bakhe8/BGL/branches
2. اضغط على تبويب "Deleted branches"
3. استعد الفرع إذا كان متاحاً (خلال 90 يوم)

أو استخدم النسخة الاحتياطية المحلية.

---

## الخلاصة

**التوصية**: الخيار 1 (تغيير الفرع الافتراضي وحذف `main`)

هذا يعطيك:
- ✅ مستودع نظيف
- ✅ فرع رئيسي واحد واضح
- ✅ سهولة في الإدارة المستقبلية
