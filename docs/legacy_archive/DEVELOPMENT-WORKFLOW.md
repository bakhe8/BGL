---
last_updated: 2025-12-17
version: 1.0
status: active
---

# استراتيجية التطوير المستمر (Development Workflow)

دليل شامل لأفضل الممارسات في تطوير مشروع BGL.

---

## 🌳 استراتيجية الفروع (Branching Strategy)

### الفرع الرئيسي

**`main`** - الفرع الرئيسي للإنتاج
- ✅ دائماً مستقر وجاهز للنشر
- ✅ يحتوي على آخر نسخة مختبرة
- ❌ **لا تطور مباشرة عليه**

---

## 📋 سير العمل الموصى به

### 1. للميزات الجديدة (New Features)

```bash
# 1. تأكد أنك على آخر نسخة من الفرع الرئيسي
git checkout main
git pull origin main

# 2. أنشئ فرع جديد للميزة
git checkout -b feature/اسم-الميزة

# مثال:
git checkout -b feature/export-to-pdf
git checkout -b feature/email-notifications
git checkout -b feature/advanced-search
```

**تسمية فروع الميزات:**
- `feature/` - للميزات الجديدة
- `enhancement/` - للتحسينات على ميزات موجودة

**أمثلة:**
```
feature/bulk-import
feature/auto-backup
enhancement/decision-ui
enhancement/performance-optimization
```

---

### 2. لإصلاح الأخطاء (Bug Fixes)

```bash
# 1. تأكد أنك على آخر نسخة
git checkout main
git pull origin main

# 2. أنشئ فرع للإصلاح
git checkout -b fix/وصف-المشكلة

# مثال:
git checkout -b fix/database-connection-error
git checkout -b fix/excel-import-fails
git checkout -b fix/blank-report-page
```

**تسمية فروع الإصلاحات:**
- `fix/` - لإصلاح الأخطاء
- `hotfix/` - لإصلاحات عاجلة في الإنتاج

---

### 3. التطوير على الفرع

```bash
# اعمل على ميزتك/إصلاحك
# عدّل الملفات حسب الحاجة

# احفظ تقدمك بانتظام
git add .
git commit -m "نوع: وصف قصير للتغيير"

# مثال:
git commit -m "feat: Add PDF export functionality"
git commit -m "fix: Resolve database locking issue"
git commit -m "docs: Update user guide with new features"
```

---

### 4. أنواع الـ Commits (Commit Types)

استخدم البادئات التالية لتنظيم الـ commits:

| البادئة | الاستخدام | مثال |
|---------|-----------|------|
| `feat:` | ميزة جديدة | `feat: Add bulk delete functionality` |
| `fix:` | إصلاح خطأ | `fix: Correct calculation in matching engine` |
| `docs:` | تحديث الوثائق | `docs: Add API documentation` |
| `refactor:` | إعادة هيكلة الكود | `refactor: Simplify database queries` |
| `perf:` | تحسين الأداء | `perf: Optimize Excel import speed` |
| `test:` | إضافة اختبارات | `test: Add unit tests for matching logic` |
| `chore:` | مهام صيانة | `chore: Update dependencies` |
| `style:` | تحسينات UI/CSS | `style: Improve decision page layout` |

---

### 5. دفع الفرع للمستودع البعيد

```bash
# دفع الفرع لأول مرة
git push -u origin feature/اسم-الميزة

# الدفعات التالية
git push
```

---

### 6. الدمج في الفرع الرئيسي

#### الطريقة الأولى: Merge مباشر (للمشاريع الفردية)

```bash
# 1. ارجع للفرع الرئيسي
git checkout main

# 2. اسحب آخر التحديثات
git pull origin main

# 3. ادمج فرع الميزة
git merge feature/اسم-الميزة

# 4. ادفع للمستودع البعيد
git push origin main

# 5. احذف الفرع المحلي (اختياري)
git branch -d feature/اسم-الميزة

# 6. احذف الفرع البعيد (اختياري)
git push origin --delete feature/اسم-الميزة
```

#### الطريقة الثانية: Pull Request على GitHub (للعمل الجماعي)

1. ادفع الفرع للمستودع البعيد
2. اذهب إلى: https://github.com/bakhe8/BGL
3. سترى رسالة "Compare & pull request"
4. اضغط عليها وأنشئ Pull Request
5. راجع التغييرات
6. اضغط "Merge pull request"
7. احذف الفرع بعد الدمج

---

## 🏷️ إدارة الإصدارات (Versioning)

### استراتيجية الأرقام

نستخدم **Semantic Versioning** (الإصدار الدلالي):

```
v[MAJOR].[MINOR].[PATCH]

مثال: v1.2.3
```

- **MAJOR** (1.x.x): تغييرات كبيرة أو غير متوافقة
- **MINOR** (x.2.x): ميزات جديدة متوافقة
- **PATCH** (x.x.3): إصلاحات أخطاء

### متى تنشئ علامة جديدة؟

#### إصدار Patch (v1.2.1, v1.2.2...)
```bash
# بعد إصلاح أخطاء صغيرة
git tag -a v1.2.1 -m "Patch release: Bug fixes"
git push origin v1.2.1
```

#### إصدار Minor (v1.3.0, v1.4.0...)
```bash
# بعد إضافة ميزات جديدة
git tag -a v1.3.0 -m "Feature release: PDF export, bulk operations"
git push origin v1.3.0
```

#### إصدار Major (v2.0.0, v3.0.0...)
```bash
# بعد تغييرات كبيرة
git tag -a v2.0.0 -m "Major release: Complete UI overhaul"
git push origin v2.0.0
```

---

## 📝 مثال عملي كامل

### السيناريو: إضافة ميزة تصدير PDF

```bash
# 1. البداية - تأكد من آخر نسخة
git checkout main
git pull origin main

# 2. أنشئ فرع الميزة
git checkout -b feature/pdf-export
# Switched to a new branch 'feature/pdf-export'

# 3. طوّر الميزة
# ... عدّل الملفات، أنشئ ملفات جديدة ...

# 4. احفظ التقدم (commit أول)
git add app/Services/PdfExportService.php
git add www/export-pdf.php
git commit -m "feat: Add PDF export service and endpoint"

# 5. استمر في التطوير
# ... عدّل الملفات ...

# 6. احفظ (commit ثاني)
git add www/assets/css/print.css
git add docs/PDF-Export-Guide.md
git commit -m "feat: Add print styles and documentation for PDF export"

# 7. اختبر الميزة
# ... اختبر كل شيء ...

# 8. ادفع للمستودع البعيد
git push -u origin feature/pdf-export

# 9. ارجع للفرع الرئيسي وادمج
git checkout main
git merge feature/pdf-export

# 10. ادفع التحديثات
git push origin main

# 11. أنشئ علامة للإصدار الجديد
git tag -a v1.3.0 -m "Feature release: PDF export functionality"
git push origin v1.3.0

# 12. نظّف (احذف الفرع)
git branch -d feature/pdf-export
git push origin --delete feature/pdf-export
```

---

## 🔧 نصائح مهمة

### ✅ افعل (Do)

1. **اعمل commits صغيرة ومتكررة**
   - كل commit يجب أن يمثل تغيير منطقي واحد
   - سهولة التراجع عند الحاجة

2. **اكتب رسائل commits واضحة**
   ```bash
   # ✅ جيد
   git commit -m "fix: Resolve database locking in concurrent imports"
   
   # ❌ سيء
   git commit -m "fix bug"
   ```

3. **اسحب التحديثات بانتظام**
   ```bash
   git pull origin main
   ```

4. **اختبر قبل الدمج**
   - تأكد أن كل شيء يعمل قبل دمج الفرع

5. **استخدم `.gitignore`**
   - لا تدفع ملفات مؤقتة أو حساسة

### ❌ لا تفعل (Don't)

1. **لا تطور مباشرة على `main`**
   - دائماً أنشئ فرع جديد

2. **لا تدفع ملفات قاعدة البيانات**
   - مهيأ بالفعل في `.gitignore`

3. **لا تعمل commits كبيرة جداً**
   - صعب التتبع والتراجع

4. **لا تحذف فروع قبل دمجها**
   - قد تفقد عملك

5. **لا تنسى التوثيق**
   - حدّث الوثائق مع كل ميزة جديدة

---

## 🚀 التطوير السريع (Quick Development)

### للتغييرات الصغيرة جداً

إذا كان التغيير بسيط جداً (مثل تصحيح نص، تحديث وثيقة):

```bash
# يمكنك العمل مباشرة على الفرع الرئيسي
git checkout main
git pull origin main

# عدّل الملف
# ...

# احفظ وادفع
git add .
git commit -m "docs: Fix typo in README"
git push origin main
```

⚠️ **فقط للتغييرات البسيطة جداً!**

---

## 📊 مراجعة دورية

### أسبوعياً
- [ ] راجع الفروع المفتوحة
- [ ] احذف الفروع المدمجة
- [ ] حدّث الوثائق

### شهرياً
- [ ] راجع الإصدارات القديمة
- [ ] نظّف العلامات غير المستخدمة
- [ ] راجع `.gitignore`

---

## 🆘 حل المشاكل الشائعة

### نسيت إنشاء فرع وطورت على `main`

```bash
# احفظ التغييرات الحالية في فرع جديد
git checkout -b feature/اسم-الميزة

# التغييرات ستنتقل معك للفرع الجديد
git add .
git commit -m "feat: Add feature"

# ارجع للفرع الرئيسي
git checkout main

# التغييرات الآن في الفرع الجديد فقط
```

### تعارضات عند الدمج (Merge Conflicts)

```bash
# عند ظهور تعارضات
git merge feature/اسم-الميزة
# CONFLICT in file.php

# 1. افتح الملفات المتعارضة وحل التعارضات
# 2. بعد الحل:
git add .
git commit -m "merge: Resolve conflicts from feature/اسم-الميزة"
```

### تراجع عن commit

```bash
# التراجع عن آخر commit (لكن الاحتفاظ بالتغييرات)
git reset --soft HEAD~1

# التراجع والتخلص من التغييرات (خطر!)
git reset --hard HEAD~1
```

---

## 📚 موارد إضافية

- **Git Documentation**: https://git-scm.com/doc
- **GitHub Guides**: https://guides.github.com
- **Semantic Versioning**: https://semver.org

---

## الخلاصة

**للتطوير المستمر:**

1. 🌿 أنشئ فرع جديد لكل ميزة/إصلاح
2. 💾 احفظ commits صغيرة ومنطقية
3. 📝 اكتب رسائل واضحة
4. 🔄 ادمج في الفرع الرئيسي بعد الاختبار
5. 🏷️ أنشئ علامات للإصدارات المهمة
6. 🧹 نظّف الفروع القديمة

**تذكّر**: الهدف هو إبقاء `main` دائماً مستقر وجاهز للنشر! 🚀

