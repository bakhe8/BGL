# ملاحظات الإصدار 2.0 (Version 2.0 Release Notes)

**التاريخ:** 2025-12-19
**الحالة:** إصدار رئيسي (Major Release)

يمثل الإصدار 2.0 قفزة نوعية في معمارية نظام BGL، حيث يركز على تنظيف الكود، إعادة هيكلة الملفات، وإضافة ميزات جديدة لتحسين تجربة المستخدم.

## 🌟 الميزات الجديدة (New Features)

### 1. ميزة تمديد الضمان (Extension Request) 🆕
تم إضافة القدرة على إنشاء "طلب تمديد" (Extension Request) مباشرة من لوحة السجل التاريخي للضمان.
- **زر جديد:** تمت إضافة زر "إنشاء تمديد" (Create Extension) بجانب زر "إصدار خطاب إفراج".
- **تعبئة تلقائية:** يقوم النظام بإنشاء سجل تمديد جديد (`extension_action`) ونسخ جميع البيانات ذات الصلة من أحدث سجل للضمان تلقائياً.
- **تمييز بصري:** تظهر سجلات التمديد في الجدول الزمني (Timeline) بشارة زرقاء مميزة "تمديد" لسهولة التعرف عليها.
- **تحديثات قاعدة البيانات:** تم تحديث قاعدة البيانات لدعم نوع السجل الجديد `extension_action`.

## 🏗️ التحسينات المعمارية (Architectural Improvements)

### 1. توحيد ملفات العرض (Unified Views Structure)
تم تجميع كافة ملفات الواجهة (View files) في مجلد واحد منظم `app/Views/` بدلاً من تشتتها في الجذر.
- **الهيكل الجديد:**
  - `app/Views/pages/`: للصفحات الرئيسية (مثل `decision-page`, `stats`, `reports`).
  - `app/Views/partials/`: للأجزاء المشتركة (Headers, Navigation).
  - `app/Views/letters/`: لقوالب الخطابات.
- **نظام التوجيه (Routing):** تم إنشاء `www/includes/views-router.php` لإدارة الوصول لهذه الملفات بمسارات نظيفة (مثلاً `/stats` بدلاً من `/pages/stats.php`).

### 2. تنظيف المشروع (Project Cleanup) 🧹
تم إجراء عملية تنظيف شاملة للمشروع لإزالة الملفات القديمة وغير المستخدمة.
- **إزالة المجلدات القديمة:** تم حذف `www/views`, `www/pages`, `www/letters` القديمة.
- **أرشفة الوثائق:** تم نقل الوثائق القديمة وتقارير التحليل إلى `docs/archive/` للحفاظ على نظافة المجلد الرئيسي.
- **تحسين الجذر:** أصبح مجلد المشروع الجذري نظيفاً ويحتوي فقط على الملفات الأساسية للتشغيل والتوثيق.

## 🛠️ إصلاحات وتحسينات (Fixes & Improvements)

- **إصلاح قيود قاعدة البيانات:** تم حل مشكلة `CHECK constraint` للسماح بأنواع سجلات جديدة بسلاسة.
- **تحسينات JavaScript:** تم دمج ملفات المساعدة (`helpers.js`) لتقليل الطلبات وتحسين الأداء.
- **توحيد الروابط:** تم تحديث جميع الروابط الداخلية لاستخدام المسارات الجديدة النظيفة.

## 📝 تعليمات التحديث (Upgrade Instructions)

للترقية من الإصدار v1.x إلى v2.0:
1. تأكد من أخذ نسخة احتياطية لقاعدة البيانات (`storage/database/app.sqlite`).
2. قم بتحديث ملفات المشروع بالكامل.
3. قد يتطلب الأمر تشغيل `run_migration.php` (تم تضمينه) لترقية هيكل قاعدة البيانات إذا لم يتم ذلك تلقائياً.

---

**ملاحظة:** هذا الإصدار يضع أساساً قوياً للميزات المستقبلية ويسهل عملية الصيانة والتطوير.
