# 🗺️ خريطة الطريق (ROADMAP)

**الحالة:** Active
**آخر تحديث:** 2025-12-19
**الهدف القادم:** Phase 6 - Letter Purpose Integration

---

## 🚀 المرحلة القادمة: Phase 6 - تكامل نية الخطاب

بناءً على المراجعة المعمارية الشاملة (ديسمبر 2025)، تم تحديد الفجوة الجوهرية في النظام وهي **"غياب نية الخطاب (Letter Purpose)"** عند الإدخال.

النظام الحالي يفترض أن كل ضمان هو "تجديد" (Renewal)، بينما الواقع يتطلب دعم:
1.  **Extension** (تمديد)
2.  **Release** (إفراج)
3.  **Reduction** (تخفيض)
4.  **Confirmation** (مصادقة)

### 🛠️ خطة التنفيذ المعتمدة

#### 1. قاعدة البيانات (Database Schema)
- إضافة عمود `letter_purpose` في جدول `imported_records`.
- النوع: `TEXT` (ENUM emulation).
- القيم: `'EXTENSION'`, `'RELEASE'`, `'REDUCTION'`, `'CONFIRM'`.
- القيمة الافتراضية: `'EXTENSION'`.

#### 2. واجهة القرار (Decision UI Logic)
- **الموقع:** قسم البيانات العلوية (Data Section) - بجانب المبلغ ونوع الضمان.
- **الأداة:** قائمة منسدلة (Dropdown Selector).
- **السلوك:**
    - عند فتح السجل، تكون القيمة الافتراضية `EXTENSION`.
    - عند تغيير القيمة (مثلاً إلى `RELEASE`)، يتم تحديث حالة المعاينة (Preview) **فوراً**.

#### 3. محرك المعاينة (Dynamic Preview Engine)
- إلغاء الاعتماد على قالب "التمديد" الثابت.
- إنشاء 4 قوالب نصوص ديناميكية في `decision.js` / `decision-page.php`.
- ربط القالب المختار بقيمة `letter_purpose`.

### ✅ الهدف النهائي
تحقيق تدفق بيانات سليم:
**تحديد النية (في البيانات) ← توليد الخطاب (تلقائياً)**.
بدلاً من اختيار القالب يدوياً عند الطباعة.

---

## 📅 الأرشيف (Completed Phases)

- **Phase 1-3:** Core System & Matching Engine (Done)
- **Phase 4:** Scoring & Star Ratings (Done - v1.3)
- **Phase 5:** Unified Decision Interface & Smart Skip (Done - v1.4)
