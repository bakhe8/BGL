# نظام القرار والتعلم (Decision & Learning System)

يُعد "نظام القرار" العقل المدبر الذي يجمع بين الذكاء الاصطناعي (أو الخوارزميات الذكية) وبين القرار البشري النهائي.

## 🧠 فلسفة النظام
لا يقوم النظام باتخاذ القرارات نيابة عن المستخدم، بل يقوم بـ **"تجهيز"** القرار له.
*   **اقتراح** أفضل مورد بناءً على الاسم.
*   **ترتيب** الاقتراحات حسب الأهمية (النجوم ⭐).
*   **التعلم** من قرارات المستخدم السابقة لعدم تكرار الأسئلة.

---

## 📊 محرك التقييم والنقاط (Scoring Engine)

عندما يعرض النظام اقتراحات لمورد معين، يقوم بحساب "نقاط" لكل اقتراح لتحديد ترتيبه وعدد النجوم.

### معادلة النقاط (Scoring Formula)
```
Total Score = (Fuzzy Score × 100) + Source Weight + Usage Bonus - Penalties
```

1.  **Fuzzy Score (التشابه النصي)**:
    *   تطابق تام (Exact): 100 نقطة.
    *   بداية الكلمة (Starts With): 85 نقطة.
    *   تشابه حروف (Levenshtein): نسبة مئوية (0-100).

2.  **Source Weight (وزن المصدر)**:
    *   من التعلم (Learning): +100 (أعلى ثقة).
    *   من تاريخ المستخدم (User History): +80.
    *   من القاموس الرسمي (Dictionary): +40.

3.  **Usage Bonus (مكافأة الاستخدام)**:
    *   يتم إضافة نقاط إضافية للموردين الذين تم اختيارهم كثيراً سابقاً.
    *   القاعدة: `+25` نقطة لكل استخدام (بحد أقصى `+150`).
    *   مكافأة الحداثة (Recency): `+25` إذا تم استخدامه في آخر 30 يوم.

4.  **Penalties (العقوبات - الحظر التدريجي)**:
    *   إذا قام المستخدم بحظر مورد لاقتراح معين، يتم خصم `50` نقطة لكل مرة حظر.

### نظام النجوم (Star Rating)
بناءً على النقاط النهائية، يتم منح النجوم:
*   ⭐⭐⭐ **(3 نجوم)**: نقاط ≥ 200 (خيار ممتاز، تم استخدامه كثيراً).
*   ⭐⭐ **(نجمتان)**: نقاط 120-199 (خيار جيد، تشابه عالي).
*   ⭐ **(نجمة واحدة)**: نقاط < 120 (اقتراح قاموس عادي).

---

## 🎓 نظام التعلم (Learning System)

كيف "يتعلم" النظام؟
عندما تختار مورداً يدوياً لسجل ما، يقوم النظام بتسجيل هذا الربط في جدول `supplier_suggestions` (أو `user_decisions` سابقاً).

### سيناريو التعلم:
1.  الاسم في Excel: **"SBM"**
2.  النظام لم يتعرف عليه (0 اقتراحات).
3.  المستخدم بحث واختار: **"شركة الأعمال التجارية السعودية"**.
4.  **النظام يحفظ**: "SBM" → "شركة الأعمال التجارية السعودية".
5.  **في المرة القادمة**: عند استيراد "SBM"، سيظهر الاقتراح فوراً مع علامة "من التعلم" و 3 نجوم.

---

## 🛡️ الموردين vs البنوك (Suppliers vs Banks)

> **مفهوم تقني هام**: يتم التعامل مع الموردين والبنوك بشكل مختلف جذرياً في الهيكلية.

### الموردين (Suppliers)
*   عددهم ضخم (الآلاف).
*   يعتمدون على جدول `supplier_alternative_names` و `learning`.
*   العلاقة: One-to-Many (مورد واحد له مئات الأسماء البديلة).

### البنوك (Banks)
*   عددهم محدود ومعروف (حوالي 25 بنك).
*   يعتمدون على الأعمدة `official_name_ar`, `official_name_en`, `short_code`.
*   لا يحتاجون لتعقيد نظام التعلم، بل يعتمدون على الكلمات المفتاحية (Keywords).

---

## 🚦 واجهة القرار (Decision UI)

تتميز واجهة القرار بـ **التسلسل البصري (Visual Hierarchy)** لمساعدة المستخدم:

1.  **الشريحة الخضراء (Green Chip)**: الاختيار الحالي المحفوظ (إن وجد).
2.  **الشريحة الذهبية (Gold Chip)**: اقتراح 3 نجوم (الأكثر احتمالاً).
3.  **الشرائح العادية**: اقتراحات القاموس.
4.  **نص "من الاكسل"**: يظهر فقط إذا كان الاسم المختار يختلف عن الاسم الأصلي، لضمان المطابقة البصرية.
