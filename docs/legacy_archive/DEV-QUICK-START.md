# دليل التطوير السريع (Quick Start)

## 🎯 للميزات الجديدة

```bash
# 1. أنشئ فرع جديد
git checkout main
git pull
git checkout -b feature/اسم-الميزة

# 2. طوّر وأحفظ
# ... عدّل الملفات ...
git add .
git commit -m "feat: وصف الميزة"

# 3. ادفع واختبر
git push -u origin feature/اسم-الميزة
# ... اختبر كل شيء ...

# 4. ادمج
git checkout main
git merge feature/اسم-الميزة
git push

# 5. نظّف (اختياري)
git branch -d feature/اسم-الميزة
```

---

## 🐛 لإصلاح الأخطاء

```bash
# 1. أنشئ فرع إصلاح
git checkout main
git pull
git checkout -b fix/وصف-المشكلة

# 2. أصلح واحفظ
# ... أصلح المشكلة ...
git add .
git commit -m "fix: وصف الإصلاح"

# 3. ادمج
git checkout main
git merge fix/وصف-المشكلة
git push
```

---

## 🏷️ للإصدارات الجديدة

```bash
# بعد إضافة ميزات أو إصلاحات
git tag -a v1.X.0 -m "وصف الإصدار"
git push origin v1.X.0
```

---

## ⚡ قواعد سريعة

✅ **افعل:**
- أنشئ فرع لكل ميزة
- اكتب commits صغيرة
- اختبر قبل الدمج

❌ **لا تفعل:**
- لا تطور على `main` مباشرة (إلا للتغييرات البسيطة جداً)
- لا تدفع ملفات قاعدة البيانات

---

📚 **للتفاصيل الكاملة**: راجع [DEVELOPMENT-WORKFLOW.md](DEVELOPMENT-WORKFLOW.md)

