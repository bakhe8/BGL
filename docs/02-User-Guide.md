---
last_updated: 2025-12-14
version: 2.0
status: active
---

# 02 - دليل المستخدم (User Guide)

## مقدمة
تم تحديث النظام ليعمل من خلال واجهة موحدة **"اتخاذ القرار" (Decision Interface)**، والتي تتيح لك القيام بجميع العمليات من مكان واحد: الاستيراد، المراجعة، والاعتماد.

## 1. استيراد الملفات (Import)

النظام يدعم **3 طرق** لإدخال البيانات:

### أ. استيراد ملف Excel 📥
1. في صفحة **اتخاذ القرار**، اضغط على زر **"📥"** في الشريط العلوي.
2. سيظهر لك نموذج الرفع. اختر ملف Excel من جهازك.
3. اضغط **"رفع ومعالجة"**.
4. عند الانتهاء، سيقوم النظام تلقائياً بتحديث الجدول وعرض السجلات الجديدة.
   - **ملاحظة**: يقوم النظام تلقائياً بالتركيز على "أحدث جلسة استيراد" لتسهيل المراجعة.

### ب. الاستيراد النصي الذكي 📋 (Smart Paste)
1. اضغط على زر **📋** في الشريط العلوي.
2. الصق النص المنسوخ من الإيميل أو أي مصدر آخر.
3. سيقوم النظام باستخراج البيانات تلقائياً.
4. اضغط **"تحليل وإضافة"**.

### ج. الإدخال اليدوي ✍️ (جديد!)
1. اضغط على زر **"✍️"** في الشريط العلوي.
2. سيظهر نموذج إدخال يدوي.
3. املأ الحقول المطلوبة:
   - **إلزامي**: المورد، البنك، رقم الضمان، رقم العقد، المبلغ
   - **اختياري**: تاريخ الانتهاء، تاريخ الإصدار، نوع الضمان، ملاحظات
4. اضغط **"حفظ وإضافة"**.
5. سيتم إنشاء السجل وإعادة توجيهك إليه مباشرة.

> **متى تستخدم الإدخال اليدوي؟**
> - عندما تحتاج لإدخال سجل واحد فقط بسرعة
> - في الحالات الطارئة التي لا تتطلب ملف Excel
> - لإضافة سجلات تكميلية لجلسة موجودة


## 2. مراجعة السجلات
بعد الاستيراد، يقوم النظام بعرض السجل الأول الذي يحتاج إلى مراجعة.

### حالة السجلات
- **جاهز (Ready)**: علامة (✓) خضراء. تعني أن النظام تعرف على المورد والبنك، أو أنك قمت باعتماده يدوياً.
- **قيد الانتظار (Pending)**: علامة (!) برتقالية. تحتاج إلى تدخلك.

### كيفية العمل على السجل
يظهر لك سجل واحد في كل مرة بجميع تفاصيله (المبلغ، التاريخ، الأسماء الأصلية في الملف).

1. **البنك**:
   - يقترح النظام البنك الأقرب.
   - يمكنك الكتابة للبحث عن بنك آخر.

2. **المورد**:
   - يقترح النظام المورد الأقرب.
   - يمكنك الكتابة للبحث.
   - **إضافة جديد**: إذا كتبت اسماً ولم تجده، سيظهر زر **"إضافة ... كمورد جديد"**. اضغط عليه لإضافته فوراً للقائمة واعتماده.

3. **الحفظ والانتقال**:
   - اضغط **"حفظ السجل والانتقال إلى التالي"** (أو استخدم الاختصار `Ctrl + Enter`).
   - سيقوم النظام بحفظ قرارك، **وتعليمه للنظام** (Learning) ليتم استخدامه مستقبلاً.
   - إذا كان هناك سجلات أخرى مطابقة تماماً في نفس الملف، سيقوم النظام بتحديثها تلقائياً (Propagation).

## 3. أدوات إضافية
- **تحديث 🔄**: لإعادة تحميل البيانات من الخادم.
- **إعادة مطابقة 🔃**: لإجبار النظام على إعادة تشغيل خوارزميات المطابقة الذكية على جميع السجلات (مفيد بعد تغيير الإعدادات أو إضافة موردين جدد).
- **الفلترة**: اضغط على العدادات في الأعلى (الكل، المعتمد، الانتظار) للتنقل السريع.

## 4. الإعدادات
من صفحة الإعدادات (في القائمة الجانبية)، يمكنك التحكم في:
- الأوزان (Weights) وقواعد المطابقة.
- إدارة النسخ الاحتياطي.
- تصدير/استيراد القاموس.

