# تدفق المنطق (Logic Flow)

يشرح هذا المستند الفلسفة والمنطق الداخلي الذي يحكم عمل النظام.

## 🎯 الرؤية والمفهوم (Concept & Vision)

تم تصميم النظام لتحويل البيانات "الفوضوية" (Heterogeneous) من مصادر متعددة إلى سجلات موثوقة (Deterministic) يمكن الاعتماد عليها قانونياً.

### المبدأ الذهبي: ضمان واحد = سجل واحد
بغض النظر عن عدد مرات تكرار الضمان في ملفات Excel، أو اختلاف طرق كتابة الاسم، يجب أن ينتهي المطاف بوجود **سجل واحد فقط** لكل ضمان فعلي.

---

## 🏗️ مفاهيم المجال الأساسية (Core Domain Concepts)

### 1. سجل الضمان (Guarantee Record)
الوحدة الأساسية في النظام. يمثل عملية ضمان بنكي واحدة. هو الكائن الذي يتم عليه:
*   المعالجة (Processing)
*   المراجعة (Review)
*   التعلم (Learning)
*   الإخراج (طباعة الخطابات)

### 2. نوع الخطاب (Letter Type)
يحدد الغرض القانوني من الخطاب. الأنواع المدعومة حصراً:
*   طلب تجديد (Guarantee Renewal Request)
*   طلب تخفيض (Guarantee Amount Reduction Request)
*   طلب مصادقة معلومات (Guarantee Information Confirmation Request)
*   طلب إفراج (Guarantee Release Request)

### 3. نوع مرجع الضمان (Guarantee Reference Type)
يحدد سبب إصدار الضمان (ولا يؤثر على المعالجة، فقط على القالب):
*   ضمان قائم على عقد (Contract-Based)
*   ضمان قائم على أمر شراء (Purchase-Order-Based)

---

## ⚙️ منطق المعالجة (Processing Logic)

### خط الأنابيب الموحد (Unified Pipeline)
سواء جاءت البيانات من Excel أو لصق نصي أو إدخال يدوي، تمر جميعها في نفس الأنبوب:

1.  **تطبيع البيانات الخام**: تنظيف المدخلات.
2.  **تحليل الأرقام والتواريخ**: تحويلها لصيغ موحدة (ISO).
3.  **حل اسم البنك**: البحث عن البنك في القاموس.
4.  **حل اسم المورد**: البحث عن المورد (الخطوة الأصعب).
5.  **تحديد نوع الضمان**: (نهائي/ابتدائي/دفعة مقدمة).
6.  **التحقق من التكرار**: هل الرقم موجود مسبقاً؟
7.  **إنشاء السجل**.

### معالجة الغموض (Ambiguity Handling)
عندما يواجه النظام شكاً (مثلاً: موردين يحملان أسماء متشابهة جداً)، يتوقف عن "التذاكي" (Guessing) ويحول السجل فوراً إلى `PENDING` ليقرر الإنسان.

> **قاعدة**: تدخل الإنسان أفضل من قرار آلي خاطئ قد يترتب عليه مسؤولية قانونية.

---

## 🤖 منطق التعلم (Assisted Intelligence)

التعلم في النظام هو "مُساعد" (Assistant) وليس "صانع قرار" (Decision Maker).

*   **المصدر**: يتعلم من تكرار المدخلات الخام ومن تصحيحات المستخدم.
*   **المخرج**: تحسين دقة الاقتراحات وترتيبها في المرات القادمة.
*   **حدود**: التعلم يقترح، لكن المستخدم يملك سلطة الموافقة أو الرفض دائماً.

---

## 🔄 الدورة الكاملة (End-to-End Flow)

```
Input (Excel/Text)
  ↓
Normalize & Process
  ↓
Decision Panel (تدخل بشري عند الحاجة)
  ↓
Learn (تسجيل القرار للمستقبل)
  ↓
Generate Letter (توليد المستند القانوني)
  ↓
Output (طباعة/PDF)
```
