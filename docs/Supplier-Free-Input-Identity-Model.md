## Supplier Identity Model with Free Input & Cumulative Learning (Unified Spec)

هدف المستند: توحيد التعامل مع الموردين عند السماح بالإدخال الحر، مع الحفاظ على الهوية الثابتة، التعلم التراكمي، منع التكرار، وتجميد العرض. هذا المستند مرجعي ويعكس المنطق الجديد المعتمد: الإدخال الحر هو قرار صريح، والاختيار/الرفض يتم بقرار المستخدم (لا حفظ تلقائي بدون قرار).

---

### 1) قواعد البيانات (DB)
**الجداول الأساسية:**
- `suppliers`: الحقول المستخدمة فعلياً  
  - `official_name` (إلزامي)  
  - `normalized_name` (إلزامي)  
  - `supplier_normalized_key` (إلزامي، بدون مسافات، طول ≥ 5)  
  - `display_name` (اختياري)  
  - `is_confirmed`, `created_at`, `updated_at`

- `supplier_alternative_names`: أسماء بديلة يدوية (مطبّعة).

- `supplier_aliases_learning`: طبقة التعلم (آخر قرار فقط)  
  - `original_supplier_name`, `normalized_supplier_name` (فريد)  
  - `learning_status`: `supplier_alias` | `supplier_blocked`  
  - `linked_supplier_id` (لا يوجد blocked عام)  
  - `learning_source` (مثلاً review)  
  - `updated_at`

- `imported_records`: تجميد عرض المورد  
  - `supplier_display_name` يحفظ الاسم الرسمي لحظة القرار.

**فهارس مقترحة:**  
- `suppliers.normalized_name`, `suppliers.supplier_normalized_key`  
- `supplier_alternative_names.normalized_raw_name`  
- `supplier_aliases_learning.normalized_supplier_name`

---

### 2) التطبيع (Normalization)
الدالة الرسمية: `SupplierNameNormalizer::normalizeSupplierName($name)`  
القواعد: lowercase، إزالة الرموز، توحيد الهمزات، تبسيط التاء المربوطة، حذف الكلمات العامة (شركة، مؤسسة، est, co, ltd, trading …)، دمج المسافات.

مفتاح الهوية: `makeSupplierKey($name)` = normalizeSupplierName ثم إزالة المسافات.  
الحد الأدنى للطول بعد التطبيع: 5 حروف.

تنبيه Legacy (قبل التنفيذ):
- يجب إصلاح/توليد normalized_name و supplier_normalized_key (طول ≥5) لكل مورد قديم قبل التشغيل.
- أي مورد لا يمكن توليد key صالح له يُرفض في المطابقة حتى يُصحَّح.

---

### 3) أنبوب المطابقة (Matching Pipeline)
الترتيب الملزم:
1. **Learning أولاً**:  
   - `supplier_alias` → تطابق مباشر.  
   - `supplier_blocked` → استبعاد كل مرشح تم رفضه سابقًا (لا blocked عام).  
2. **Overrides** (كما هو).  
3. **Exact by supplier_normalized_key** (المرجع الأول للهوية).  
4. **Exact by normalized_name**.  
5. **Exact بدائل** (`supplier_alternative_names`).  
6. **Fuzzy**:  
   - Strong ≥ 0.90  
   - Weak ≥ 0.80  
   - < 0.80 يُهمل.  
النتيجة تُعرض؛ الحفظ يتطلب قرارًا صريحًا (اختيار/رفض/إنشاء) ولا يتم تلقائياً.

---

### 4) التعلم التراكمي (Learning)
- يحدث فقط عند قرار المستخدم:
  - اختيار مرشّح → تسجيل `supplier_alias` للمورد المختار.
  - رفض مرشّح أو كتابة نص مختلف بوجود مرشحات → تسجيل `supplier_blocked` لكل المرشحين الظاهرين (≥0.80)، لا blocked عام.
- لا تعلم تلقائي أثناء الاستيراد أو بدون قرار.
- آخر قرار يكتب فوق السابق (ON CONFLICT / UPSERT) لكل normalized_supplier_name بحيث يبقى أحدث قرار هو الحقيقة.

---

### 5) التجميد (Display Freeze)
- عند قرار نهائي: `supplier_display_name = official_name` للمورد المرتبط.
- العرض في الواجهة:  
  1) `supplier_display_name` إن وجد.  
  2) وإلا الاسم الرسمي للمورد المرتبط.  
  3) وإلا الاسم الخام من Excel.
- تعديل القاموس لاحقاً لا يغيّر السجلات القديمة.

---

- الحقل قابل للكتابة + Dropdown للمرشحين من نفس API الحالية (`/records/{id}/candidates`)؛ القرار بيد المستخدم.
- المنطق الجديد (واجهة فقط):
  - إذا وُجد أي مرشح بدرجة ≥0.80:
    - يجب الاختيار أو الرفض صراحة؛ لا حفظ تلقائي ولا إنشاء جديد بوجود مرشحات.
    - الكتابة بنص لا يطابق أي مرشح ظاهر تُعد رفضًا صريحًا لكل المرشحين الظاهرين (blocked لكل مرشح ≥0.80) ثم إعادة المطابقة.
    - يمكن للمستخدم تجاوز ذلك بالضغط على “إنشاء مورد جديد”، لكن السيرفر يطبّق حارس الطول والتكرار (normalize، key بلا مسافات، طول ≥5، similarity <0.90 على supplier_normalized_key) وقد يرفض.
  - إذا لم يوجد أي مرشح ≥0.80:
    - يمكن الإنشاء مباشرة من الحقل نفسه، مع نفس شروط السيرفر (normalize، key بلا مسافات، طول ≥5، similarity <0.90 على supplier_normalized_key) ورسالة منع واضحة إذا فشلت الشروط.
- لا حفظ خام بلا ربط؛ كل حفظ = اختيار (alias) أو رفض (blocked) أو إنشاء جديد بقرار المستخدم.

حماية من الأخطاء عبر الواجهة:
- الحفظ يتم دوماً بقرار آلي (اختيار/alias أو إنشاء جديد)؛ لا يُسمح بحفظ نص خام دون ربط.
- الإنشاء أو التعديل الرسمي من القاموس يبقى بنفس قيود التطبيع ومنع التكرار.

---

### 7) منع التكرار (Dedup)
- similarity ≥ 0.90 عند إنشاء/تعديل مورد → منع صارم (رفض الحفظ).  
- طول الاسم بعد التطبيع ≥ 5.  
- supplier_normalized_key إلزامي (بدون مسافات).
- التشابه لمنع الإنشاء يُحسب على supplier_normalized_key حصراً؛ normalized_name لا يُستخدم في قرار المنع.

---

### 8) السيناريوهات المتوقعة (ملخص)
- مرشّح ≥0.80 ومطابق بعد التطبيع → يختار المستخدم المرشح، يُسجَّل alias، يُجمَّد العرض.
- مرشّح ≥0.80 ومختلف قليلاً → يختار أو يرفض المستخدم؛ الاختيار = alias، الرفض = blocked لكل المرشحين الظاهرين؛ لا إنشاء جديد بوجود مرشحات.
- لا مرشحين ≥0.80 → يمكن إنشاء مورد جديد مباشرة من الحقل (مع فحص الطول والتكرار، والتشابه على normalized_key <0.90).
- تصحيح/ترجمة للاسم مع مرشحات → alias؛ بدون مرشحات → إنشاء جديد بشروط.
- إدخال حر لا يلوث القاموس: alias/blocked فقط بقرار المستخدم مع وجود مرشح، إنشاء جديد فقط عند انعدام أي تطابق.

---

### 9) توصيات قبل وأثناء التنفيذ
- ضمان أن واجهة الحفظ لا تُرسل نصًا خامًا بلا ربط: السيرفر يقرر تلقائياً (اختيار/alias أو إنشاء جديد).
- تسجيل بسيط (logging) للنص الخام + القرار الناتج (auto-select/alias/new) لتتبع السلوك.
- التأكد من تطبيق makeSupplierKey عند كل إنشاء/تعديل مورد.
- مراجعة أي موارد legacy بلا normalized_name وتصحيحها أو حذفها إذا بلا مراجع.

---

### 10) خلاصة التنفيذ
- المنطق الخلفي ثابت: التطبيع، matching، التعلم، منع التكرار، التجميد.  
- الواجهة فقط تصبح Editable + Dropdown دون أن تغيّر أي سلوك سيرفري.  
- كل قرار للمستخدم يُترجم إلى alias أو blocked عبر الـ API الحالية، مع تجميد العرض.  
- السجلات القديمة تبقى ثابتة، الجديدة تتبع القاموس والتعلم المحدث.

### ملحق: مبادئ إضافية وأهم السيناريوهات (دمج النص الإضافي)

**أساسيات الهوية والتطابق:**
- `normalizeSupplierName` تطبّع كل الأسماء (Excel أو إدخال المستخدم).
- `supplier_normalized_key = removeSpaces(normalizeSupplierName(official_name))` إلزامي، طول ≥5، ويستخدم قبل normalized_name في التطابق.
- ترتيب المقترحات: Learning → normalized_key → normalized_name → alternatives → Fuzzy (Strong≥0.90، Weak≥0.80، ما دون ذلك يُهمل).

**التعلم (alias / blocked):**
- يسجل alias عند اختيار مرشح، وblocked عند رفضه. لا تعلم تلقائي من الكتابة.
- لا blocked عام: كل blocked مرتبط بـ supplier_id.
- التعلم تدريجي وآمن (آخر قرار فقط).

**الإدخال الحر (Editable Input):**
- حالتان:
  - إذا وُجدت مرشحات ≥0.80: يجب اختيار/رفض صراحة؛ لا إنشاء مورد جديد.
  - إذا لم توجد مرشحات (<0.80): يمكن الضغط على زر “إنشاء مورد جديد” بعد فحص التطبيع والطول ومنع التكرار ≥0.90.
- الحفظ يُرسل فقط `supplier_id` أو `supplier_blocked_id`. لا إنشاء مورد أو alias أو blocked دون قرار صريح.
- supplier_display_name لا يعتمد على النص الحر، بل على الاسم الرسمي للمورد المختار أو المورد الجديد بعد إنشائه بقرار المستخدم.

**منع التكرار:**
- طول الاسم بعد التطبيع ≥5.
- similarity ≥0.90 يمنع الإنشاء (تكرار).
- normalized_key بدون مسافات وفريد قدر الإمكان.

**سلوك رئيسي لكل سيناريو:**
- اسم جديد تماماً → إنشاء مورد جديد (بعد الفحص).
- اسم مشابه ≥0.80 → يجب اختيار أو رفض؛ الاختيار = alias، الرفض = blocked.
- حفظ بدون اختيار/رفض → يجب منعه أو تحذير المستخدم.
- تغيير النص بعد اختيار مرشح → يتطلب إعادة اختيار لتفادي التضارب.

**ملخّص الحماية:**
- الحفظ بالـ IDs فقط.
- لا إنشاء موارد/aliases/blocked من الواجهة؛ كل ذلك يمر بالقاموس أو /decision.
- التطبيع وحساب key في السيرفر فقط.
- التجميد يحفظ استقرار السجلات القديمة دائماً.

---

### مخطط مبسط (نصي) لتدفق القرار
1) المستخدم يكتب → السيرفر يطبّع → يعيد مرشحين (Learning → Key → Normalized → Alt → Fuzzy).
2) إذا يوجد مرشح ≥0.80 → المستخدم يختار أو يرفض:  
   - اختيار مرشح ⇒ `supplier_id` ⇒ تسجيل alias + تجميد العرض  
   - رفض مرشح ⇒ `supplier_blocked_id` ⇒ تسجيل blocked  
3) إذا لا مرشحين ⇒ يسمح بإنشاء مورد جديد مع فحص الطول والتكرار.  
4) السجلات تُجمَّد بالاسم الرسمي للمورد المختار، والتعلم يطبَّق في كل استيراد لاحق.  

### مخطط قرار مبسط (Supplier Decision Flow)
```
flowchart TD

%% 1) User Input
A[📝 User Free Text Input<br/>Supplier Input Field] --> B{Normalize<br/>Input}
B --> C[normalized_input]

%% 2) Matching Pipeline
C --> D{Matching Pipeline<br/><br/>1) Learning<br/>2) Key Match<br/>3) Normalized Match<br/>4) Alt Names<br/>5) Fuzzy ≥0.80}
D -->|Candidates Found<br/>(score ≥ 0.80)| E[📋 Candidate List<br/>(Exact / Alt / Strong / Weak)]
D -->|No Candidates<br/>(score < 0.80)| N{New Supplier<br/>Required?}

%% 3A) Candidates Found   (CASE A)
E --> F{User Action<br/>With Candidates}
F -->|Select Candidate| G[✔ Create Alias<br/>→ alias saved in learning]
G --> H[📌 Freeze Display Name]
F -->|Reject Candidate| I[🚫 Create Blocked Entry<br/>→ blocked saved in learning]
I --> H
F -->|User Edits Text<br/>But Makes No Selection| J[❗ Cannot Save<br/>Prompt User to Select or Reject]
H --> Z[🏁 END]

%% 3B) No Candidates Found  (CASE B)
N -->|normalized length < 5| R[❗ Reject: Name Too Short]
N -->|similarity ≥ 0.90| S[❗ Reject: Too Similar to Existing Supplier]
N -->|Else (unique new name)| T[🆕 Create New Supplier<br/>(by explicit user action)<br/>+ normalized_key<br/>+ normalization rules]
T --> U[📌 Freeze Display Name]
U --> Z

%% 3C) Translation Case (CASE C)
C --> D2{Text Changed but Candidates Still Found?}
D2 -->|Yes| E
D2 -->|No| D

%% 4) Back-End Safety
Z --> X[🔒 Back-End Enforcement:<br/>• No official names saved from free text<br/>• Only IDs saved (supplier_id / blocked_id)<br/>• alias/blocked only from explicit decisions<br/>• new supplier creation only in CASE B]
```

                 ┌─────────────────────────────────┐
                 │   المستخدم يكتب في Supplier-Input │
                 └─────────────────────────────────┘
                                  │
                                  ▼
                    ┌──────────────────────────┐
                    │ normalizeSupplierName(raw)│
                    └──────────────────────────┘
                                  │
                                  ▼
      ┌─────────────────────────────────────────────────────────┐
      │  إرسال النص المطبّع إلى السيرفر للحصول على المرشحين  │
      │         GET /candidates?supplier_name=...             │
      └─────────────────────────────────────────────────────────┘
                                  │
                                  ▼
                   ┌──────────────────────────┐
                   │ السيرفر: ترتيب المطابقة │
                   └──────────────────────────┘
                                  │
        ┌────────────────────────────────────────────────────────────┐
        │ 1) تعلّم (Learning):                                       │
        │    • إن وجد alias → تطابق نهائي                           │
        │    • إن وُجد blocked → استبعاد المورد المعني فقط         │
        └────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
        ┌────────────────────────────────────────────────────────────┐
        │ 2) Exact by normalized_key                                 │
        │ 3) Exact by normalized_name                                │
        │ 4) Alternative matches                                     │
        └────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
        ┌────────────────────────────────────────────────────────────┐
        │ 5) Fuzzy Matching                                          │
        │      Strong ≥ 0.90                                         │
        │      Weak   ≥ 0.80                                         │
        │  < 0.80 → لا يظهر                                         │
        └────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
                ┌────────────────────────────────┐
                │ هل يوجد مرشّح ≥ 0.80 ؟         │
                └────────────────────────────────┘
                        │                     │
               YES (يوجد)              NO (لا يوجد)
                        │                     │
                        ▼                     ▼
          ┌──────────────────────┐   ┌─────────────────────────────┐
          │ عرض قائمة المرشحين  │   │ لا يوجد أي تطابق = نص جديد │
          │ Exact / Alt / Fuzzy  │   └─────────────────────────────┘
          │ Strong / Weak        │                │
          └──────────────────────┘                ▼
                        │              ┌──────────────────────────────┐
                        │              │ السماح بإنشاء مورد جديد فقط │
                        │              │ لأن التطابق صفر             │
                        │              └──────────────────────────────┘
                        │                       │
                        │                       ▼
                        │         ┌────────────────────────────────────────┐
                        │         │ إنشاء مورد جديد بعد التحقق:           │
                        │         │  • normalizeSupplierName               │
                        │         │  • makeSupplierKey (بدون مسافات)      │
                        │         │  • طول ≥5                               │
                        │         │  • similarity ≥0.90 ❌ يمنع الإنشاء   │
                        │         └────────────────────────────────────────┘
                        │
                        ▼
       ┌──────────────────────────────────────────────┐
       │ المستخدم يتخذ قرارًا من ثلاث احتمالات فقط:  │
       └──────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 1) ✔ اختيار مرشح (مورد موجود)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        ▼
┌──────────────────────────────────────────┐
│ POST /decision مع supplier_id            │
│                                          │
│ • النظام يسجل supplier_alias            │
│   لأن المستخدم ربط هذا النص بالمورد     │
│ • freeze → supplier_display_name         │
│ • التعلم يضمن عدم مراجعة هذا الاسم      │
└──────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 2) ❌ رفض مرشح (Blocked)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        ▼
┌──────────────────────────────────────────┐
│ POST /decision مع supplier_blocked_id    │
│                                          │
│ • يسجّل التعلم: supplier_blocked         │
│ • لن يظهر هذا المورد لهذا النص لاحقًا   │
└──────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 3) 🆕 إنشاء مورد جديد (عند عدم وجود مرشحين فقط)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        ▼
┌──────────────────────────────────────────┐
│ POST /dictionary/supplier/create         │
│                                          │
│ Checks:                                  │
│  • normalized length ≥ 5                 │
│  • similarity ≥0.90 تمنع                 │
│  • key = normalized_without_spaces       │
│                                          │
│ ثم يعود إنشاء المورد الجديد              │
└──────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# مرحلة التجميد Freeze (منع تغيّر السجل بعد الحفظ)


supplier_display_name ← official_name للمورد المختار


لا يتغير أبدًا حتى لو تغير المورد.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# مرحلة التعلم التراكمي (يؤثر على كل الملفات القادمة)

بعد كل قرار:

- alias → يثبّت المورد الصحيح  
- blocked → يمنع المرشح الخاطئ  
- النظام يصبح أدق في كل استيراد لاحق  
- عدد السجلات “needs review” ينخفض تدريجيًا  

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 🔚 ملخص التدفق في شكل نقاط

1. يكتب المستخدم → النظام يطبّع  
2. النظام يجلب المرشحين  
3. إذا لم توجد مرشحات ≥0.80 → المورد جديد  
4. إذا وجدت مرشحات → يجب اختيار أو رفض  
5. الاختيار = alias  
6. الرفض = blocked  
7. الإنشاء الجديد يخضع لمنع التكرار  
8. السجل يُجمّد اسمه دائمًا  
9. التعلم يطبق دائمًا قبل كل مطابقة لاحقة  
