# ملاحظات الإصدار 1.3 (Version 1.3 Release Notes)
**التاريخ:** 2025-12-17
**الحالة:** Production Readiness (جاهز للإنتاج)

## ملخص التحديث
هذا الإصدار يمثل خطوة كبيرة نحو استقرار التطبيق وتبسيط بنيته التحتية. التركيز الأساسي كان على دمج واجهة اتخاذ القرار لتصبح هي الواجهة الرئيسية للنظام، مع إعادة هيكلة طبقة الـ API لضمان الأداء والأمان.

## 🚀 التغييرات الرئيسية

### 1. دمج الواجهة (UI Merge)
*   **دمج `decision.php` في `index.php`:** أصبحت صفحة اتخاذ القرار هي الصفحة الرئيسية (`/`) للنظام.
*   تم حذف ملف `decision.php` القديم لمنع الازدواجية والتشتت.
*   تم تحديث جميع الروابط الداخلية (مثل Javascript) لتشير إلى الجذر `/` بدلاً من `/decision.php`.

### 2. استعادة وإصلاح الـ API (Back-end Restoration)
*   **إعادة بناء `API Router`:** تم تضمين موجه (Router) مخصص لطلبات الـ API في بداية ملف `index.php` لمعالجة الطلبات القادمة من الواجهة (AJAX) مثل:
    *   حفظ القرارات (`/api/records/{id}/decision`).
    *   إضافة الموردين (`/api/dictionary/suppliers`).
    *   إعادة الحساب (`/api/records/recalculate`).
*   **منع تداخل HTML:** الـ Router الجديد يضمن إرجاع `JSON` فقط ويمنع تسرب أي كود HTML للاستجابة.

### 3. تحسينات قاعدة البيانات (Database Integrity)
*   **إنشاء `App\Support\Database`:** فئة مساعدة جديدة لتوحيد عملية الاتصال بقاعدة بيانات SQLite.
*   **إصلاح `guarantee-history.php`:**
    *   تم تعديل استعلامات SQL لتتوافق مع أسماء الجداول الصحيحة (`imported_records` بدلاً من `records`).
    *   تم استبدال الأعمدة غير الموجودة (`updated_at` -> `created_at`, `short_name` -> `official_name`).
    *   إضافة ميزة تتبع تغييرات **نوع الضمان** و **تاريخ الانتهاء**.

## 🐛 إصلاحات الأخطاء (Bug Fixes)
*   إصلاح خطأ `500 Internal Server Error` عند حفظ القرار بسبب غياب الـ Router.
*   إصلاح خطأ `SQLSTATE[HY000]` عند البحث في تاريخ الضمان.
*   إصلاح خطأ `Fatal Error` الناتج عن استدعاء دالة `connection()` غير الموجودة (تم توفير Alias لها).

## 📝 تعليمات الترقية
1.  تأكد من أخذ نسخة احتياطية لقاعدة البيانات `storage/database/app.sqlite`.
2.  اسحب التغييرات من المستودع (`git pull origin main`).
3.  لا توجد تغييرات مطلوبة في بنية قاعدة البيانات (No Migrations needed)، حيث تم التعامل مع الفروقات برمجياً.

---
**فريق التطوير BGL**
