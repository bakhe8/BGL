# قاموس المصطلحات (Glossary)

تعريفات للمصطلحات التقنية والإدارية المستخدمة في النظام.

## 🏛️ الكيانات الأساسية

### الضمان (Guarantee)
تعهد كتابي صادر من البنك بناءً على طلب العميل بدفع مبلغ معين للمستفيد عند المطالبة.

### السجل (Record)
التمثيل الرقمي للضمان داخل النظام. كل صف في جدول `imported_records` هو سجل.

### الجلسة (Session)
مجموعة من السجلات التي تم استيرادها في عملية واحدة (ملف واحد أو عملية لصق واحدة). تساعد في تجميع العمليات.

---

## ⚙️ مفاهيم النظام

### المطابقة (Matching)
عملية العثور على المورد الرسمي والبنك الرسمي الذي يطابق النصوص الخام الواردة في الملفات.

### التطبيع (Normalization)
معالجة النص لإزالة الاختلافات الشكلية (مثل المسافات والهمزات) لجعل المقارنة ممكنة.

### اللقطة (Snapshot)
نسخة محفوظة من بيانات السجل في لحظة زمنية معينة، تُحفظ كـ JSON داخل سجل الأحداث التاريخية.

### الحظر التدريجي (Progressive Blocking)
آلية لتقليل ترتيب اقتراح معين تدريجياً (بدلاً من إخفائه فوراً) عند قيام المستخدم برفضه عدة مرات.

### الاسم الخام (Raw Name)
الاسم كما ورد حرفياً في ملف Excel أو النص المدخل، قبل أي معالجة.

### الاسم الرسمي (Official Name)
الاسم المعتمد في قاعدة البيانات (جدول `suppliers` أو `banks`) والذي سيظهر في الخطابات الرسمية.
