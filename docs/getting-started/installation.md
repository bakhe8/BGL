# دليل التثبيت والتشغيل (Installation Guide)

يوضح هذا الدليل كيفية إعداد وتشغيل نظام إدارة الضمانات (BGL) في بيئة محلية.

## 📋 المتطلبات (Requirements)
*   **PHP**: إصدار 8.1 أو أحدث.
*   **الإضافات المطلوبة (PHP Extensions)**:
    *   `sqlite3`
    *   `pdo_sqlite`
    *   `fileinfo` (لرفع ملفات Excel)
    *   `gd` (اختياري، للصور)
*   **Composer**: لإدارة الحزم (اختياري، المكتبات مضمنة في `vendor` بالفعل).

---

## 🚀 خطوات التثبيت (Installation Steps)

### 1. تحضير المشروع
قم بنسخ مجلد المشروع إلى جهازك.

```bash
git clone <repository-url> BGL
cd BGL
```

### 2. تثبيت الاعتمادات (Dependencies)
إذا لم يكن مجلد `vendor` موجوداً، قم بتشغيل:
```bash
composer install --no-dev
```

### 3. إعداد قاعدة البيانات
النظام يستخدم SQLite، لذا لا حاجة لتثبيت خادم قاعدة بيانات منفصل. ملف القاعدة موجود في `storage/database/app.sqlite`.

لتهيئة الجداول لأول مرة (أو للتحديث)، قم بتشغيل سكربتات التهجير (Migrations):

```bash
# تشغيل التهجيرات الأساسية
php scripts/migrate_timeline_events.php
php scripts/migrate_add_display_names.php
```

---

## ▶️ التشغيل (Running the Application)

النظام مصمم للعمل باستخدام خادم PHP الداخلي (Built-in Server) لسهولة الاستخدام.

### تشغيل الخادم
```bash
php -S localhost:8000 server.php
```

الآن افتح المتصفح على العنوان: `http://localhost:8000`

---

## 🛠️ استكشاف الأخطاء (Troubleshooting)

### مشكلة: صفحات بيضاء أو أخطاء 500
*   تأكد من صلاحيات الكتابة في مجلد `storage/`.
*   تأكد من صلاحيات الكتابة لملف `storage/database/app.sqlite`.
*   راجع ملف السجلات: `debug.log`.

### مشكلة: "Driver not found"
*   تأكد من تفعيل `extension=pdo_sqlite` في ملف `php.ini`.

### مشكلة: أحرف غريبة في الأسماء العربية
*   النظام يستخدم ترميز `UTF-8` بالكامل. تأكد من أن ملفات Excel المحفوظة بترميز صحيح، رغم أن النظام يحاول معالجة ذلك تلقائياً.
