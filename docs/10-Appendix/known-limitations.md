# حدود معروفة
- لا يوجد مزامنة سحابية أو مستخدمون متعددون؛ نسخة واحدة محلية فقط.  
- لا يوجد نظام صلاحيات/أدوار.  
- لا يدعم استيراد PDF/OCR، فقط Excel/لصق/يدوي.  
- SQLite قد لا يتحمل حجم بيانات ضخم جدًا (ملايين الصفوف) بدون ضبط.  
- لا يوجد تحديث تلقائي؛ التحديث يتم يدويًا عبر حزم الإصدار.
