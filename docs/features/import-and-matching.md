# الاستيراد والمطابقة (Import & Matching)

هذا المستند يشرح الرحلة التي تقطعها البيانات منذ لحظة دخولها النظام وحتى استقرارها كسجلات في قاعدة البيانات.

## 📥 طرق الإدخال (Input Methods)

يستقبل النظام البيانات عبر 3 قنوات تصب جميعها في مصب واحد (`ImportedRecordRepository`):
1.  **استيراد Excel**: معالجة ملفات كاملة (دفعة واحدة).
2.  **Smart Paste**: معالجة نصوص غير مهيكلة (تحليل ذكي).
3.  **Manual Entry**: إدخال فردي عبر نموذج.

---

## ⚙️ معالجة البيانات (Processing Pipeline)

### 1. التطبيع (Normalization)
قبل أي مقارنة، يتم تنظيف النصوص لضمان دقة البحث.
*   **توحيد الحروف**: `أ إ آ` -> `ا`، `ة` -> `ه`، `ى` -> `ي`.
*   **إزالة الزوائد**: إزالة المسافات المزدوجة، الرموز الخاصة `- _ /`.
*   **تجاهل الكلمات الشائعة**: (شركة، مؤسسة، Ltd, Co, LLC) لا تؤثر في المطابقة.

### 2. محرك المطابقة (Matching Engine)

يقوم `MatchingService` بمحاولة العثور على المورد والبنك المناسبين.

#### خوارزميات البحث (بالترتيب):
1.  **Exact Match**: تطابق حرفي (بعد التطبيع).
2.  **Aliases & Learning**: البحث في الأسماء البديلة المحفوظة سابقاً.
3.  **Fuzzy Search**: استخدام خوارزمية `Levenshtein Distance` للعثور على أقرب اسم (إذا كانت نسبة التشابه > 80%).
4.  **Token Match**: تشابه الكلمات (مثلاً "مطعم البيك" تشبه "البيك للمأكولات" لوجود كلمة "البيك").

### 3. حالات المطابقة (Match Status)

بناءً على نتيجة البحث، يتم تحديد حالة السجل:

| الحالة (Status) | الشرط | الإجراء |
|-----------------|-------|---------|
| `ready` (جاهز) | تطابق > 90% (أو تم العثور عليه في التعلم) | تم تحديد المورد، جاهز للاعتماد. |
| `needs_review` | تطابق بين 60% - 90% | النظام يقترح مورداً، لكن يطلب تأكيد المستخدم. |
| `pending` (معلق) | تطابق < 60% | لم يتم العثور على أي تطابق، على المستخدم البحث يدوياً. |

---

## 🧠 الكشف عن التعارضات (Conflict Detection)

يقوم النظام بفحص السجلات للتأكد من منطقيتها:
*   **تعارض المورد**: هل المورد المختار يمتلك حقاً هذا الرقم من الضمان؟ (يتم التحقق عبر سجلات سابقة).
*   **تعارض البنك**: هل البنك المختار يطابق صيغة رقم الضمان؟ (بعض البنوك لها صيغ ثابتة لأرقام الضمانات).

في حال اكتشاف تعارض، يتم وضع علامة `flag` على السجل لتنبيه المستخدم.

---

## 📝 اللصق الذكي (Smart Paste Feature)

ميزة متقدمة تعتمد على `Regular Expressions` (Regex) لاستخراج البيانات من النصوص الخام.

### استراتيجية الاستهلاك (Consumption Strategy)
لضمان الدقة، يقوم المحلل "بأكل" النص تدريجياً:
1.  يبحث عن **المبلغ** -> يستمخرجه ويحذفه من النص.
2.  يبحث عن **التاريخ** -> يستمخرجه ويحذفه.
3.  يبحث عن **رقم الضمان** (نمط محدد) -> يستمخرجه ويحذفه.
4.  يبحث عن **اسم البنك** (كلمات مفتاحية) -> يستمخرجه ويحذفه.
5.  **المتبقي من النص** غالباً هو اسم المورد.

هذه الاستراتيجية تمنع الخلط (مثلاً: عدم اعتبار رقم الضمان جزءاً من اسم الشركة).
